// Conditional Ternary
let isStuding = true;
let subject = isStuding? "Maths" : "Computer";
console.log(`I am studing ${subject}.`);

let goingForShopping = false;
let onWhichDay = goingForShopping? "monday" : "sunday";
console.log(`we are going for shopping on ${onWhichDay}.`);

let doingWork = "onComputer";
let timeSpend = doingWork? "2 hours" : "3 hours";
console.log(`He is doing work on computer for ${timeSpend}.`);

let scoredMarks = 780;
let totalMarks = scoredMarks? "780" : "110";
console.log(`Ali's obtain marks in final is ${totalMarks}.`);